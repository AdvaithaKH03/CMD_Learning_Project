from settings import *

def law_1():
    print(result_1)
    print(result_2)

    b = input("> ")

    if b == "Example":
        print(f" a = {result_3}")
        print(f" b = {result_4}")
        print("Now the formula is: a x b =  ab integer")
        print("Answer: ab =")
        print(result_5)

    else:
        print("Invalid input for the second prompt.")

def law_2():
    print(result_6)
    print(result_7)

    b = input("> ")

    if b == "Example":
        print(f"Number one = {result_8}")
        print(f"Number Two = {result_9}")
        print("Now the formula is: a x b = b x a it has the same answer")
        print("Answer:")
        print(f"Number one = {result_8}")
        print(f"Number Two = {result_9}")
        print(f"Formula part one which is a x b = {result_10}")
        print(f"Formula part two which is b x a = {result_11}")
        print("Now both answers are the same this verifies the property.")

    else:
        print("Invalid input for the second prompt.")

def law_3():
    print(result_12)
    print(result_13)

    b = input("> ")

    if b == "Example":
        print(f"Number one = {result_14}")
        print(f"Number Two = {result_15}")
        print(f"Number Three = {result_16}")
        print("Now the formula is: (a x b) + c = (b x c) + a it has the same answer")
        print("Answer:")
        print(f"Number one = {result_17}")
        print(f"Number Two = {result_18}")
        print(f"Formula part one which is (a x b) x c = {result_17}")
        print(f"Formula part two which is (b x c) x a = {result_18}")
        print("Now both answers are the same this verifies the property.")

    else:
        print("Invalid input for the second prompt.")

def law_4():
    print(result_19)
    print(result_20)

    b = input("> ")

    if b == "Example":
        print(result_21)
        print(result_22)
        print("Now both answers show the usage of the property.")

    else:
        print("Invalid input for the second prompt.")